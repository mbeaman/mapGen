/target
**/*.rs.bk
Cargo.lock.bak

# WASM build output
/web/pkg
/web/node_modules
/web/dist

# Generated worlds & maps
/worlds
/maps

# IDE
.vscode
.idea
*.swp
.DS_Store

# Env
.env
.env.local

# Per-machine scratch — never commit. Project session state and
# resume context live in tracked files now (`docs/SESSION.md`,
# `AGENTS.md`, `CONTRIBUTING.md`); this directory is reserved for
# ad-hoc throwaways (debug renders, local notes, throwaway scripts).
/.local/
