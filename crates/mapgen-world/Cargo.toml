[package]
name = "mapgen-world"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Geography pipeline: plates, noise, erosion, hydrology, climate, biomes, society."

[dependencies]
mapgen-core = { path = "../mapgen-core" }
mapgen-geom = { path = "../mapgen-geom" }
mapgen-history = { path = "../mapgen-history" }
noise = { workspace = true }
rand = { workspace = true }
rand_chacha = { workspace = true }
serde = { workspace = true }
smallvec = { workspace = true }
indexmap = { workspace = true }
libm = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
serde_json = { workspace = true }
ciborium = { workspace = true }
blake3 = { workspace = true }
# For the perf_baseline example only (render + sector budgets). A dev-dep, so
# the normal build graph is unaffected and the cycle with render's own dev-dep
# on this crate is permitted by Cargo.
mapgen-render = { path = "../mapgen-render" }
