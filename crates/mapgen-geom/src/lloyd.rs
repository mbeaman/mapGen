//! Lloyd relaxation is performed by `voronoice` directly via
//! `set_lloyd_relaxation_iterations`. This module is reserved for a
//! hand-rolled variant if we ever need finer control (e.g. weighted
//! relaxation for coastal repacking in Phase 2).
