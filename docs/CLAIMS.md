# Claims registry

Every claim we make about the generator — in commit messages, in
`docs/inter_continental_design.md`, in `.local/sessionstate.md`, in the README —
should map to **one layer** and to **one test that goes red if the claim becomes
false**. This file is that map.

It exists because of a failure that has recurred four times: a test passes at a
*lower* layer (the data is correct) and the claim quietly inflates to a *higher*
one (you can see it). The mod-5 exclave-invisibility shipped as "done" for
exactly this reason — a data-layer test was green, so "the data is right" was
sold as "you can see the sundering." See `[[feedback_false_green_tests]]` and the
"verify the observable, not the data" lesson.

## The rule

> **A feature is not "done at layer L" until a test exists at layer L.**

If a row's test cell says **GAP**, the claim is *not yet validated at that layer*
— do not describe it as done there. A GAP is a unit of work, not a footnote.

## The layers

| Layer | Proves | Asserted where |
|---|---|---|
| **Determinism** | byte-reproducibility & cross-platform identity (the contract under everything) | blake3 goldens, `cross_platform.rs`, `fmath_purity.rs` |
| **Data** | the `WorldData` is semantically correct after the real `Pipeline` runs | Rust `tests/*.rs` stepping the pipeline on canonical seeds |
| **Replay** | the time channel (`border_changes`) actually *evolves* — the thing animates | year-over-year deltas in history / e2e slider |
| **Observable** | the rendered artifact (SVG / raster / DOM) *shows* the thing | `svg_invariants.rs`, `visual_regression.rs`, Playwright e2e |

**Canonical seeds** live in `crates/mapgen-testsupport` (`REFERENCE_SEED=42`,
`CROSSING_SEEDS=[11,19,26,30]`, `SUNDERED_SEEDS=[23,10]`, `COLONIZE_SEEDS=[18,27,32]`,
`SIZABLE_BODY_MIN=24` — the carrier seeds were RE-DERIVED for the periodic planet, see
the periodic-gen section) and in the param builders `reference_params` / `planet_params`.
Reference them by name, never as a magic literal.

The **Red-mutation** column is the one-line change that *should* flip the test
red — it is the proof the test is not false-green. Each new claim test must ship
with its red-mutation recorded here and verified once by hand.

---

## Determinism contract (cross-cutting)

| Claim | Layer | Seed | Test | Red-mutation |
|---|---|---|---|---|
| Same seed → byte-identical world | Determinism | 42 | full `WorldData`: golden `tests/golden/seed42_full.blake3.txt` (via `pipeline_spec.rs::full_pipeline_golden_hash`) — the load-bearing cite. Geography run-to-run only: `determinism.rs::same_seed_same_world` (2000 cells, `generate()`, no society/history) | perturb any `StageRng` draw or a `BTree` iteration order |
| Different seed → different world | Determinism | 42 vs other | `determinism.rs::different_seed_different_world` | ignore the seed in any stage |
| Native ≡ wasm32 byte-identical (full pipeline) | Determinism | 42 | `mapgen-wasm/tests/cross_platform.rs::full_pipeline_golden_hash_matches_native_under_wasm` | replace an `fmath` call with raw `f32::sin` in a stage |
| Native ≡ wasm32 byte-identical (refined sector) | Determinism | 42 sector | `cross_platform.rs::refined_sector_golden_matches_native_under_wasm` | change a refine-path transcendental |
| No raw transcendentals in any `src/` | Determinism | — | `mapgen-history/tests/fmath_purity.rs` | add `x.sin()` / `.powf()` in any crate `src` |
| Pipeline stepping ≡ `generate_full` (coarse) | Data | 42 | `pipeline_spec.rs::stepper_matches_generate_full` | skip a stage in the stepper |
| Pipeline stepping ≡ `generate_full` (fine) | Data | 7 | `pipeline_spec.rs::fine_stepper_matches_generate_full` | merge two fine erosion sub-steps |
| `WorldData` round-trips through JSON | Data | 42 | `determinism.rs::full_world_round_trips_through_json` | drop a `#[serde]` field |

## Foundation — landmass-distinct society (schema v19)

| Claim | Layer | Seed | Test | Red-mutation |
|---|---|---|---|---|
| Each culture is instanced per landmass (same archetype on 2 continents → 2 ids) | Data | planet | `cultures_spec.rs::cultures_are_instanced_per_landmass` | revert `populate` to the global roster build |
| Polities are confined to one landmass **at gen-time** (0 spanning) | Data | planet | `cultures_spec.rs::polities_are_confined_to_one_landmass_yet_the_planet_is_populated` (stepped to `PipelineStage::Polities`) | stamp control by global culture id again |
| The planet stays earned-sparse (land controlled, not gutted) | Data | planet | same test (≥80% sizable-body land) | confine cultures to a single cell each |
| No culture instance spans a sea-lanes body (predicate regression guard) | Data | planet | `cultures_spec.rs::no_culture_instance_spans_a_sea_lanes_body` — cultures & sea_lanes now share the one canonical land predicate `> 0.0`, so this holds by construction; the test guards against re-diverging | re-introduce `>= 0.0` in `cultures::populate` so a 0.0 cell could bridge two `>0.0` bodies |
| seed 42 (single landmass) is a byte-identical no-op under v19 | Determinism | 42 | the goldens above (re-anchored for the version byte only) | make instancing fire on a single-body world |

## Sea-lane substrate (Phase 1 Step 3a)

| Claim | Layer | Seed | Test | Red-mutation |
|---|---|---|---|---|
| Every crossing seed grows a both-tier lane graph (a crossable strait `≤40` **and** an open-ocean wall `>40`) | Data | 11, 19, 26, 30 | `sea_lanes_spec.rs::every_crossing_seed_grows_lanes_spanning_both_tiers` (each periodic crossing seed carries both a crossable strait and an open-ocean wall) | clamp `min_naval` to a single tier |
| A one-cell strait forms a lane the sea-scan alone would miss | Data | synthetic | `sea_lanes_spec.rs::one_cell_pinch_strait_forms_a_lane_the_sea_scan_alone_would_miss` | drop the sea→land pinch scan |
| A strait maps to crossable, open ocean to a wall | Data | synthetic | `sea_lanes_spec.rs::synthetic_fixture_maps_strait_to_crossable_and_ocean_to_wall` | invert the cost gate |
| Lanes are deterministic for a fixed seed | Determinism | 19 | `sea_lanes_spec.rs::lanes_are_deterministic_for_a_fixed_seed` | key the lane heap on a non-stable tiebreak |
| Sundered seeds yield no earned crossing (lanes exist but are gated too-expensive) | Data | 23, 10 | `sundered_lanes_claims.rs::no_cross_water_conquest_on_any_sundered_seed` pins the *outcome*. The mutation proof showed seed 23 *does* chart inter-body lanes (they'd carry `[0,2,8]` with the gate off) — so "sundered" = gated, not laneless. The substrate-level "no lane crossable at achievable naval" is confirmed but not a standing assertion | (see the carrier gate row) |
| Sundered seeds carry only walls (no crossable lane) | Data | 23, 10 | covered by the carrier outcome test (`sundered_lanes_claims.rs::no_cross_water_conquest_on_any_sundered_seed`) — see the carrier section | (see carrier gate row) |
| Only the open ocean bridges landmasses — no inland lake forges a lane (latent-hazard guard) | Data | 11, 19, 26, 30, 23, 10 | `sea_lanes_spec.rs::only_the_open_ocean_bridges_landmasses_no_inland_pool_does` (mutation-verified). The algorithmic fix is deferred — strait ≡ bridging-lake topologically here; needs ocean-connectivity geometry | a non-dominant `<=0.0` sea pool comes to border ≥2 sizable bodies |

## Carrier — beachhead cross-water conquest (Phase 1)

| Claim | Layer | Seed | Test | Red-mutation |
|---|---|---|---|---|
| Earned cross-water **conquest** fires on every crossing seed | Data | 11, 19, 26, 30 | `sundered_lanes_claims.rs::cross_water_conquest_fires_on_every_crossing_seed` — keys on `an_earned_overseas_seizure` (filters `from:Some`), so it pins a *conquest* specifically and a colonization can't green it (seed 11 is in both sets). Mutation-verified: disabling the beachhead reds it while the colonization test stays green | disable the beachhead in `resolve_war` |
| **No** earned crossing on any sundered seed — the naval gate is load-bearing | Data | 23, 10 | `sundered_lanes_claims.rs::no_cross_water_conquest_on_any_sundered_seed` (mutation-verified: removing the gate makes seed 23 span `[0,2,8]`) | remove the `lane.min_naval > naval_a` gate |
| Colonization settles UNCLAIMED far shores (`from:None`) and nowhere else | Data | 18, 27, 32 (fires); 19, 26, 30, 23, 10 (absent) | `sundered_lanes_claims.rs::colonization_settles_unclaimed_far_shores_and_nowhere_else` (mutation-verified both ways: disable→fires red; colonize owned anchors→absent red). The 2nd carrier — a polity *settles* an unclaimed lane far-anchor (`loops/colonization.rs`, `LoopId::Colonization`), vs the beachhead's `from:Some` conquest | disable the colonization loop / drop the `is_none` far-anchor gate |
| Exact per-seed crossing counts | Data | 11, 19, 26, 30 | **GAP (intentional, won't fix)** — *presence* is pinned per seed; exact counts are deliberately not (any history tweak shifts them → brittle regression gate) | n/a |
| The earned crossing flips in at a specific year under the slider's reconstruction | Replay | 11, 19, 26, 30 | `sundered_lanes_claims.rs::the_earned_crossing_replays_faithfully_in_the_time_slider` (mutation-verified: off-by-one in `control_at_year` flips it red; uses the slider's own `control_at_year` — earned cell is *not* the conqueror's at year-1, *is* at year, and `control_at_year(last) == society.control`) | `>` → `>=` in `control_at_year` |
| Scrubbing the slider reveals the overseas exclave region (web/DOM) | Replay+Observable | 26 @ 2000 | `web/e2e/smoke.spec.ts::scrubbing the slider reveals an overseas exclave region` — the wash now groups realms as `<g class="realm" data-polity="N">` and exclaves as `<g class="realm exclave" data-polity="N">`; the e2e asserts `.planet-political .exclave[data-polity]` ≥1 at the present and 0 at the slider min (founding). Backed by the Rust render test below | drop the `exclave` tag in `planet.rs::render_political` |
| The wash groups each realm and tags exclaves faithfully | Observable | 11, 19, 26, 30, 23, 10 | `political_legibility.rs::the_wash_groups_each_realm_and_tags_overseas_exclaves` (mutation-verified: forcing `class="realm"` trips it). Exclave-tagged polities == polities spanning ≥2 sizable landmasses; wash polygon count == controlled land cells | force `class="realm"` for all groups |
| Bordering realms (incl. the overseas exclave) render in distinct colors | Observable | 11, 19, 26, 30 | `political_legibility.rs::{bordering_realms_render_in_distinct_colours, the_overseas_exclave_is_colour_distinct_from_the_realms_it_borders, the_political_wash_renders_exactly_the_realms_legible_colours}` (mutation-verified: disabling `recolor_political` reverts the adjacency test to red). FIXED by post-history greedy graph-coloring (`polities::recolor_political`) replacing mod-5 `polity_color` | disable `recolor_political` |
| Every territorial change visibly flips a cell's color in the slider | Replay+Observable | 4@2k, 11, 19, 7 | `political_legibility.rs::every_territorial_change_flips_the_rendered_colour` (mutation-verified: dropping the from↔to edges reverts it to red). A conqueror that retreats is no longer present-adjacent to its victim, so present-adjacency alone froze the slider — caught by the seed-4 e2e; fixed by also joining `from`↔`to` of every `border_change` in the coloring graph | drop the `border_change` from↔to edges in `recolor_political` |

## Diffusion — a faith crosses water (Phase 2)

| Religions are confined to one landmass at gen-time (no faith pre-crosses oceans) | Data | 11, 19, 26, 30, 23, 10 | `diffusion_claims.rs::religions_are_confined_to_one_landmass_at_gen_time` (stepped to `PipelineStage::Religions`; mutation-verified: dropping the body filter makes a faith span). `religions::found` confines alignment-spread to the founder's body | drop the `home_body[r] != cell_body` filter in `religions::found` |
| A faith crosses water EARNED over a crossable lane — and only there | Data | 11, 19, 26, 30 (fires); 23, 10 (absent) | `diffusion_claims.rs::diffusion_carries_a_faith_across_water_only_over_a_crossable_lane` (mutation-verified both ways: disable the loop → fires red; drop the naval gate → sundered seed 23 spans `[0,1]` → absent red). `loops/diffusion.rs` (`LoopId::Diffusion`) crosses crossable lanes + spreads inland over history | disable the Diffusion loop / drop the `min_naval` gate |
| The Faith lens surfaces a crossing on the map | Observable | 11, 19, 26, 30 | `faith_overlay.rs::the_faith_wash_surfaces_every_faith_that_crossed_water` (mutation-verified: disable `render_faith` → red). The planet `planet-faith` wash draws each faith as a `data-religion` group; every religion spanning ≥2 bodies appears. e2e `smoke.spec.ts::the Faith lens swaps in the faith wash` (the Faith preset sets `on-faith`, flipping the ornate `layer-faith` group `display` none→inline) | disable `render_faith` |
| The Diffusion loop records its conversion timeline (chronological + faithful) | Data | 11, 19, 26, 30 | `diffusion_claims.rs::the_diffusion_timeline_records_every_conversion_faithfully` (mutation-verified: drop the `record_conversion` push → empty timeline → red). Each conversion lands in `history.faith_changes`; the present faith of every recorded cell == its last recorded `to`; every water-crossing faith appears as a recorded `to` | drop the emission in `loops/diffusion.rs::record_conversion` |
| `religion_at_year` replays a growing partial spread from a confined founding | Replay | 11, 19, 26, 30 | `diffusion_claims.rs::religion_at_year_replays_a_growing_partial_spread` (mutation-verified: ignore the year → all frames == present → red). Converted-cell count grows strictly founding < mid < present; the reconstructed founding has 0 faiths spanning ≥2 bodies (every crossing earned). Faith twin of `control_at_year` | `>` → `false` (ignore year) in `WorldData::religion_at_year` |
| Scrubbing the Faith slider animates a faith spreading over water (web/DOM) | Replay+Observable | seed 9 @ 2000 (web); 11, 19, 26, 30 (render) | render: `faith_overlay.rs::the_planet_faith_wash_redraws_for_a_past_year` (the `planet-faith` wash differs founding↔present — tracks `religion_id`). e2e: `smoke.spec.ts::planet Faith slider animates a faith spreading over water` (Faith lens on, scrub min→present, `.planet-faith` changes). Both mutation-verified: dropping the faith swap in wasm `renderAtYear` → red | drop the `religions.religion_id` swap in `render_at_year` |

> **Faith replay — DONE (was the surfacing residual).** The faith wash now animates over years: the Diffusion loop records `faith_changes` (v20), `WorldData::religion_at_year` reconstructs the past faith map, and wasm `renderAtYear` swaps it in alongside control so the slider replays the spread. Span widened (`replay_year_span`) so faith outlasting the last war still extends the slider. Remaining Phase-2 deferral: embargoes.

## Trade & embargo — sea routes enrich (and war severs) realms (Phase 2)

| Claim | Layer | Seed | Test | Red-mutation |
|---|---|---|---|---|
| A trade route opens across a crossable lane between two realms — and only there | Data | 11, 19, 26, 30 (fires); 23, 10 (absent) | `trade_claims.rs::trade_routes_open_across_crossable_lanes_and_only_there` (mutation-verified: disable the loop → crossing seeds emit no `TradeRouteOpened` → red). The `Trade` loop (`LoopId::Trade`, last in ORDER) opens a route the first year two DISTINCT, non-belligerent owners hold a crossable lane (`min_naval ≤ 40`); ZERO on sundered seeds (no crossable lane). Probe-picked: every non-sundered seed opens 2–6 routes, sundered = 0 | disable the `Trade` loop / drop the `min_naval` gate |
| A route lifts EXACTLY the two partners' capacity, no one else (targeting) | Data (loop) | fixture | `loops/trade.rs::a_route_lifts_exactly_the_two_partners_and_no_one_else` (mutation-verified: disable → no lift → red). A lane 0↔1 lifts capacity[0]+[1] by the trade gain, leaves off-lane [2] untouched, records the route once, emits one event; ticking again doesn't re-boost. Plus `a_wall_lane_opens_no_route` + `one_owner_of_both_anchors_opens_no_route` (the gate + exclave guards) | boost the wrong polity / drop the `pa == pb` guard |
| A capacity recompute (conquest, colonization) keeps the trade bonus | Data (loop) | fixture | `loops/trade.rs::a_capacity_recompute_keeps_the_trade_bonus` (mutation-verified: drop the bonus in `effective_capacity` → collapses to territory → red). All recompute sites route through `effective_capacity` = `polity_capacity` + `trade_bonus`, so a border change can't erase trade prosperity | `effective_capacity` returns `polity_capacity` alone |
| War between trade partners severs their route (embargo) | Data | 11, 19, 26, 30 | `trade_claims.rs::war_between_trade_partners_severs_their_route_with_an_embargo` (mutation-verified: disable the sever → no `EmbargoImposed` → red). `EmbargoImposed` fires on every crossing seed (a cross-water war IS a trade-pair war), and `embargo ≤ trade` (can't sever a route that never opened). `mearsheimer::resolve_war` records the pair in `belligerents` | disable the sever branch in the `Trade` loop |
| An embargo severs ONLY the belligerents' route, reversing its exact bonus (targeting) | Data (loop) | fixture | `loops/trade.rs::war_embargoes_only_the_belligerents_route` (mutation-verified: disable the sever → capacity not reversed → red). A 4-polity / 2-lane fixture: the belligerent pair's route is severed (exact bonus reversed, one `EmbargoImposed`), the peaceful route is untouched, and the severed route does NOT re-open. The sundered-seed direction is vacuous for embargo (no lane → no route), so THIS is the discriminator | sever the wrong route / re-open a severed route |

> **Trade & embargo surfacing — DONE in Phase 3 (below).** Capacity/population were SimState-internal; Phase 3 added the `Nation::prosperity` channel (v21) + a prosperity heatmap, a trade-route lens, chronicle weaving, (increment 2) the **first-contact arc** — the narrator weaves a sea route's birth AND its severance into one Sundered-Lane chronicle — (increment 3) the **landmass place tag** — inter-continental events now carry the far continent they reached (`Event::far_shore`, v22), and a faith's first water-crossing (`FaithCrossed`) is a chronicle that NAMES the shore it touched — (increment 4) the **multi-strand far-shore weave** — a colony (`CityFounded`) and a beachhead conquest (`Siege`) now carry `far_shore` too, and the `auto-shore` chronicle weaves EVERY strand that reached one landmass (faith, colony, sword) into a single chronicle naming the shore — and (increment 5) **cross-lens correlation** — the Trade lens tints each crossable lane by the prosperity of the realms it connects (the same ramp the Prosperity wash uses), making the trade→prosperity loop legible across lenses. Temporal surfacing of the diffusion/faith timeline shipped separately (the Replay thread's `render_at_year` + the frontend time-slider). The remaining arc work is realm tagging (`far_realms`, still no consumer) and the chronicle trade-route strand (architecturally out — lore can't map a realm to a continent at load time).

## Phase 3 surfacing — the society sim made visible

Three parallel-built, then integrated + adversarially reviewed, surfacing features (prosperity heatmap, trade-route lens, chronicle weaving).

| Claim | Layer | Seed | Test | Red-mutation |
|---|---|---|---|---|
| Final population is carried into `Nation::prosperity`, normalized + spread | Data | 11, 19, 26, 30 | `prosperity_spec.rs::final_population_is_carried_into_prosperity_and_normalized` (mutation-verified BOTH ways: skip write-back → all 0 → trips `any_positive`; constant `1.0` write-back → no spread → trips the `< 1.0` assertion). Pins populated + max==1.0 (normalized) + some realm < 1.0 (real spread, not a constant). Schema v21, goldens re-anchored | write-back `= 0.0` / `= 1.0` (constant) |
| The prosperity wash colours each realm by its prosperity (planet + ornate) | Observable | fixture | `prosperity_overlay.rs::{equal_prosperity_paints_one_fill_but_unequal_paints_two, ornate_equal_prosperity_paints_one_fill_but_unequal_paints_two}` (mutation-verified: a pid-keyed ramp leaves the EQUAL case painting two fills → trips). EQUAL prosperity → exactly ONE fill (colour is a pure function of `Nation::prosperity`, not polity id); UNEQUAL → two. Both render styles | key the ramp on `pid` instead of `nation.prosperity` |
| The Trade lens draws one line per crossable lane (planet + ornate) | Observable | 11, 19, 26, 30 | `trade_overlay.rs::{the_planet_trade_group_draws_a_line_per_crossable_lane, the_ornate_trade_layer_draws_a_line_per_crossable_lane}` (mutation-verified: neuter the render → 0 lines → trips). `==` count (tied to the world's own crossable-lane count) catches over- AND under-inclusive filters. e2e `smoke.spec.ts::the Trade lens swaps in the sea lanes` pins the display swap. Gate `MAX_CROSSABLE_NAVAL` shared across both render paths | neuter `render_trade_routes` / drop the `min_naval` gate |
| The Prosperity lens swaps in on the frontend (web/DOM) | Observable | — | `smoke.spec.ts::the Prosperity lens swaps in the prosperity wash` — the off-by-default `.layer-prosperity` group flips computed display none→inline under `on-prosperity` (the ornate LAYER_STYLE gate), not just the root class | mistype the `svg.on-prosperity .layer-prosperity` rule |
| The chronicle narrator surfaces the inter-continental contact story | Observable | 11 | `mapgen-lore/tests/contact.rs::{auto_contact_selects_an_inter_continental_contact_event, template_draft_renders_contact_into_prose_mentioning_the_route}` (mutation-verified: swap the `auto-contact` filter to war kinds → returns a war / prose lacks "route" → trips). `select_focal("auto-contact")` prefers `TradeRouteOpened`/`EmbargoImposed`; "auto"/"auto-major-war" (the war path) unchanged | filter `auto-contact` on `is_war` instead of `is_contact` |
| **First-contact arc (inc. 2):** an embargo cites the exact `TradeRouteOpened` it severs (the causal spine) | Data | 11, 19, 26, 30 (+ loop fixture) | `loops/trade.rs::an_embargo_cites_the_route_opening_it_severs` (mutation-verified: drop `.causes(&[open_id])` in the sever loop → the embargo carries no cause → trips). The opening's `EventId` is threaded through scratch `SimState::trade_routes`; the embargo cites EXACTLY it (`assert_eq!` exclusivity), so the lore engine's causal closure of the embargo is a clean two-beat `{opening, embargo}` | remove `.causes(&[open_id])` in the `Trade` sever loop |
| **First-contact arc (inc. 2):** the weave narrates a route's birth AND its severance, framed (and a war chronicle stays plain) | Observable | 11 (+ synthetic) | `mapgen-lore/tests/contact.rs::{the_first_contact_arc_weaves_the_routes_birth_and_its_severance, a_non_contact_chronicle_gets_no_sundered_lane_framing}` + `template.rs::tests::{an_open_only_arc_gets_the_birth_beat_but_not_the_severance, a_born_and_died_arc_gets_both_beats}` (mutation-verified 6×: drop the cause link → closure collapses, no birth beat; drop `FIRST_CONTACT_BEAT` → "two peoples met" trips; drop `SEVERANCE_BEAT` → "sundered once more" trips; weaken `first_contact && severed` → severance leaks onto an unsevered route → trips; make framing unconditional → a war chronicle gets framed → the negative test trips). Both beats woven chronologically under a "Sundered Lane" title; "sundered" appears in no event summary nor register frame, so it proves the framing came from the weave | drop a framing beat / weaken the `&& severed` guard / make framing unconditional |
| **Landmass place tag (inc. 3):** a faith's first water-crossing fires a `FaithCrossed` tagged with the far continent it reached (`Event::far_shore`) — only where lanes cross, never on a laneless world, and at most once per (faith, continent) | Data | 11, 19, 4 (+ 42 zero) (+ loop fixture) | `mapgen-world/tests/faith_crossing_claims.rs::a_faith_crossing_fires_a_named_milestone_where_lanes_cross_and_never_otherwise` (BOTH directions: seed42 = 0; crossing seeds ≥1, every event's `far_shore` a valid `world.continents` index) + `loops/diffusion.rs::tests::a_faith_reaching_one_continent_over_two_anchors_announces_it_once` (a synthetic 3-cell world forces the multi-anchor case the planet seeds can't — the dedup the integration test would assert vacuously). Mutation-verified: delete the emit → crossing seeds drop to 0 → trips; drop the `crossed_faiths.insert` guard → the synthetic test fires twice → trips. The far continent is resolved at Naming (back-filled onto the lane, same `connected_bodies` + `MIN_CONTINENT_DIVISOR` filter that orders `world.continents`) | delete the `FaithCrossed` emit / drop the dedup guard |
| **Landmass place tag (inc. 3):** the faith-crossing chronicle NAMES the far shore it reached (the place tag's first consumer) | Observable | 11 (+ synthetic) | `mapgen-lore/tests/contact.rs::the_faith_crossing_chronicle_names_the_continent_it_reached` + `template.rs::tests::a_faith_crossing_names_the_far_shore_from_the_tag` (mutation-verified: drop the template far-shore beat → the continent name vanishes → trips; stop resolving `far_shore` in `narrate` → trips). The discriminator: the continent name is in the chronicle but NOT in the event summary (which says only "a far shore"), so it reached the page ONLY because the narrator read `Event::far_shore` and `world.continents` resolved it. `select_focal("auto-faith")` picks the `FaithCrossed`; `ner_lexicon` includes continent names (LLM-path grounding; `lexicon_includes_named_continents`) | drop the far-shore beat / stop resolving `far_shore` in `narrate` |
| **Multi-strand far-shore weave (inc. 4):** overseas colonies (`CityFounded`) and cross-water conquests (the beachhead `Siege`) carry the far continent they reached in `Event::far_shore`, so one landmass can be reached by faith AND colony AND sword | Data | synthetic (logic) + 27 (real) | `mapgen-lore/tests/shore.rs::{select_shore_picks_a_three_strand_far_shore_on_the_diverse_seed, the_far_shore_chronicle_weaves_all_three_strands_each_naming_the_shore, a_laneless_world_has_no_far_shore_to_chronicle}` — the 3-strand classifier + weave run against a DRIFT-IMMUNE synthetic `three_strand_world()` (one tagged event per strand → `distinct_strands()==3`); the laneless guard rides a real SUNDERED seed (`select_shore` is `None`). The real OCCURRENCE on a generated world is pinned in `lore_cli.rs` (seed 27, see the periodic-gen section). Colony tag = the colonized far anchor's continent (`lane.continent_a/b`); sword tag = the beachhead loser-anchor's continent, set ONLY when a beachhead is seized. (Was real seed 9 "Duv" pre-periodic-flip; periodicity made the natural 3-strand rare, so the LOGIC test went synthetic.) | drop a strand event from `three_strand_world()` → `distinct_strands` 3→2 → trips |
| **Multi-strand far-shore weave (inc. 4):** the far-shore chronicle (`auto-shore`) weaves EVERY strand that reached one landmass — faith, colony, sword — into a single chronicle that NAMES the shore (the multi-strand consumer of `far_shore`) | Observable | synthetic + 27 | `mapgen-lore/tests/shore.rs::the_far_shore_chronicle_weaves_all_three_strands_each_naming_the_shore` (synthetic, drift-immune) + `template.rs::tests::shore_draft_weaves_a_beat_for_each_strand_present_and_no_others` (mutation-verified: make a beat unconditional → it leaks onto a shore lacking that strand → trips). The discriminator: the shore name appears in NONE of the strand event summaries, so every "…upon the shore of {shore}" beat could only come from the weaver reading the tags. `select_shore` picks the most strand-diverse shore (max distinct strands, tie→event count→lowest continent index); `narrate_shore` weaves one gated beat per present strand and degrades gracefully to a single-strand chronicle | make a strand beat unconditional → the unit test trips |
| **Cross-lens correlation (inc. 5):** the Trade lens tints each crossable sea lane by the prosperity of the realms it connects — through the SAME ramp the Prosperity wash uses — so the trade→prosperity loop is legible across lenses | Observable | 19 (+ 23 zero) | `mapgen-render/tests/trade_prosperity.rs::{the_trade_lens_tints_each_lane_by_the_prosperity_it_connects, a_sundered_world_draws_no_lanes_to_tint}`, asserted for BOTH the planet `planet-trade` wash AND the ornate `layer-trade` layer. seed 19's 4 crossable lanes span avg connected-realm prosperity ~0.13→0.97; the richest-bound lane renders DEEPER (lower RGB sum, the ramp's rich end) than the poorest. A sundered seed (23) grows no crossable lane → nothing to tint. Both `render_trade_routes` paths tint via the same `prosperity_color` / `PROSPERITY` ramp the wash uses (one colour source); render-only, so no schema/golden move | drop the per-lane stroke → all lanes share one colour → trips (both styles); invert the tint → the richest lane is no longer the deepest → the direction assertion trips (both styles) |
| **Hardening (inc. 4 review gap i):** the multi-strand far-shore chronicle survives the whole CLI round-trip — `mapgen generate --planet` persists a LANED planet world and `mapgen lore --event auto-shore` weaves it, NAMING the shore | Observable (CLI e2e) | 27 (planet) | `mapgen-cli/tests/lore_cli.rs::lore_auto_shore_weaves_the_far_shore_chronicle_through_the_cli` — drives the binary: `generate --planet --seed 27` (→ `GenerateParams::planet(27)`, a three-strand shore "Zuk" under the periodic planet + naming re-calibration) → gzip → `lore --event auto-shore`, captures the shore from the `# The Annal of the Reaching of {shore}` title and asserts all three strand beats (faith/colony/sword) name it. The library weave LOGIC is pinned (drift-immune) by `shore.rs`'s synthetic fixture; this pins the thin CLI glue (`--planet` persist + the `auto-shore` route) AND that the 3-strand chronicle genuinely OCCURS in a generated world. (Was seed 9 "Duv" pre-periodic-flip.) | drop the colony/sword `far_shore` tag → seed 27 loses that beat → trips |
| **Hardening (inc. 4 review gap ii):** the `Event::far_shore` tag fires byte-identical native↔wasm — previously only STRUCTURAL, as every cross-platform golden was seed42-laneless (far_shore elided, never exercised) | Determinism (golden) | 9 (planet) | `mapgen-world/tests/pipeline_spec.rs::planet_seed9_far_shore_golden_hash` (native) + `mapgen-wasm/tests/cross_platform.rs::planet_seed9_far_shore_golden_matches_native_under_wasm` (wasm), BOTH against the one committed golden `golden/seed9_planet_full.blake3.txt`. seed 9 is the canonical THREE-strand far shore, so its `far_shore = Some(continent)` values fire from every gen carrier (faith/colony/sword) and ride the hashed path (`Option<u16>`, serialized when `Some`); both platforms hash the whole 18k-cell planet world to that golden → native == wasm. Verified green under `wasm-pack test --node` here, not just CI | flip the golden, or a carrier transcendental that bypasses `fmath` → the wasm hash diverges → trips |

> **Determinism (Phase 3).** Only prosperity touches the hashed path: a `pop / max_pop` f32 divide (correctly-rounded, no transcendental) in the history write-back + the serialized `Nation::prosperity` field (schema **v21**, three goldens re-anchored). The trade lens (render-only, SVG unhashed) and chronicle (lore, unhashed) leave the goldens untouched. Native↔wasm byte-identity of the divide is the same boundary as the rest of the Turchin backbone — pinned by `cross_platform.rs` in CI.
>
> **Determinism (first-contact arc, inc. 2).** No schema bump, no re-anchor. The embargo→opening cause link rides scratch `SimState::trade_routes` (an `EventId` added to its value tuple — `SimState` has no `Serialize` derive) and serializes only as `cause_ids` on crossing-seed `EmbargoImposed` events; **seed42 is laneless, so the `Trade` loop emits no contact event and the goldens are byte-identical** (the seed42 golden is the empirical proof — it passed unchanged). Emitting the event draws no RNG, so no other loop's sub-stream moves. The weave is lore-only (unhashed, native-only `#![cfg(not(target_arch = "wasm32"))]`).
>
> **Determinism (landmass place tag, inc. 3 — schema v22).** Same no-op-on-seed42 shape as v20: `Event::far_shore` is `#[serde(default, skip_serializing_if = "Option::is_none")]`, the `FaithCrossed` event never fires on laneless seed42, and the `SeaLane` continent tags are `#[serde(skip)]` build-time scratch (off the hashed path entirely) — so the three goldens are **byte-identical at v21 with all v22 code in place** (the non-perturbation proof, run before bumping), and the v22 re-anchor moves the `schema_version` byte alone. The Naming back-fill is purely additive (no refactor of the body classifier → continent NAMES unmoved, the proof's load-bearing check). The emit draws no RNG and rides the lane-then-direction iteration order; `crossed_faiths` is a scratch `BTreeSet` (no `HashSet` reaches output) — so the new u16/u32-only path is native↔wasm-safe. Crossing-seed place tags (which no golden covers) are pinned by `faith_crossing_claims.rs`.
>
> **Determinism (multi-strand far-shore weave, inc. 4).** No schema bump, no re-anchor: `Event::far_shore` already exists (v22) and the new tags ride the existing `CityFounded`/`Siege` kinds — so the three goldens are **byte-identical** (seed42 is laneless → no colony/conquest crosses water → no tag fires → `skip_serializing_if` omits the field; the empirical non-perturbation proof, the goldens passed unchanged). The carriers draw NO new RNG (the colony/beachhead tags are read off already-computed `lane.continent_a/b`), add no `HashSet`/transcendental, and stamp only a `u16` continent index — so the tag-firing path is native↔wasm-safe by construction. **Residual — NOW CLOSED (hardening, inc. 4 review gap ii).** The tag-FIRING native↔wasm byte-identity was previously *structural only* (every cross-platform golden was seed42-laneless, so `far_shore` was elided and never exercised). It is now pinned by a real **laned planet golden** on seed 9 (`golden/seed9_planet_full.blake3.txt`): the native `pipeline_spec.rs::planet_seed9_far_shore_golden_hash` and the wasm `cross_platform.rs::planet_seed9_far_shore_golden_matches_native_under_wasm` hash the same laned planet world (its faith + sword carriers fire `far_shore` tags) to the same golden, so the `far_shore = Some(idx)` values are byte-identical native↔wasm (verified green under `wasm-pack test --node`; re-anchored at the periodic flip and again at the naming re-calibration). The weave itself is lore-only (unhashed, native-only `#![cfg(not(target_arch = "wasm32"))]`).

## Planet & globe presentation

| Claim | Layer | Seed | Test | Red-mutation |
|---|---|---|---|---|
| Planisphere is a Mollweide oval; drill unproject stays correct | Observable | — | `web/src/sector.test.ts` (mollweide project/unproject duality) + shared vector pin (`crates/mapgen-render` + web) | perturb the Mollweide forward constant in one place only |
| A planet generates and a continent drill refines (≥ L2) | Observable | 8 | `smoke.spec.ts::generates a planet, then drills into a continent` (seed 8 @ 2000 — under the periodic planet its largest continent is ~1/11 of the world AND its centroid sits on land, so the label click resolves via `continentAt` and depth-sizes to L2; seed 4's largest continent merged bigger and its centroid fell in the sea → grid-drill → L1) | make the drill no-op |
| `?scale=planet` permalink reloads as a planet | Observable | — | `smoke.spec.ts::?scale=planet permalink reloads as a planet` | drop the permalink read |
| The globe renders a frame and applies the texture | Observable | — | `smoke.spec.ts::globe scale mounts a 3D sphere and renders a frame` (`data-rendered`/`data-textured`) | never call `renderer.render` |
| The globe locks the style control & survives scale toggling | Observable | — | `smoke.spec.ts::globe locks the style control and survives scale toggling` | leak the GL context on swap |
| The political wash animates over years | Replay | 4 | `smoke.spec.ts::planet time-slider animates political control` + `svg_invariants.rs::planet_style_washes_in_political_control_and_animates_with_history` | freeze the wash to year 0 |
| The globe wears a richer EQUIRECT, FONTLESS texture (parchment + political wash + coast + rivers) — not the flat biomes v1, not the Mollweide oval | Observable | 11 | `mapgen-render/tests/globe_texture.rs::globe_texture_is_equirect_fontless_and_washes_realms` (equirect 2048×1024, no `@font-face`/`<text>`, has `planet-political`, and a far-NE point Mollweide would pinch) + `planet.rs::tests::equirect_projection_is_identity` (`Proj::equirect` is identity; Mollweide pinches a far-NE point) | switch `render_globe_texture` to the Mollweide `Proj` (far-NE point vanishes) / remove the `equirect` short-circuit in `project` |
| The globe time-slider re-textures the sphere per year (empires animate on the globe) | Observable (replay) | 4 | `smoke.spec.ts::globe time-slider re-textures the sphere per year` — the slider shows at the globe root and a scrub bumps the monotonic `data-textures` count + the year label leaves "present" | re-add the `&& !globeScale` gate (slider hidden on the globe) → trips |
| The globe supports lens overlays (Faith/Prosperity/Trade), as the 2D planisphere does | Observable | 11 (render) / 11 (e2e) | `globe_texture.rs::globe_texture_is_equirect_fontless_and_washes_realms` (the lens wash groups `planet-faith`/`planet-prosperity`/`planet-trade` + their `svg.on-*` swap CSS are baked into the texture) + `smoke.spec.ts::globe lens toggle re-textures the sphere` (toggling a lens in globe mode bumps `data-textures` — the sphere re-rasterizes with the `on-<lens>` class injected, vs a DOM restyle) | drop the lens groups/CSS from `render_globe_texture` → render test trips; skip `retextureGlobe` in `setLayerState`'s globe branch → the e2e's texture count never bumps |
| A globe drill STAYS in 3D — no 2D handoff; the camera flies into a free-fly view over the region (increment 1a, supersedes the old "hands off to 2D") | Observable | — | `smoke.spec.ts::globe scale mounts a 3D sphere and renders a frame` — after a drill `data-region="1"` AND `#globe-canvas` visible AND `#map-content` hidden (the exact INVERSE of the old "stepped aside to a 2D sector"), breadcrumb `/L[1-6]/` | restore `exitGlobeView()+requestRefine()` in `navTo`'s globe branch → `#map-content` shows / canvas hides → trips |
| The free-fly globe camera frames a region in its LOCAL up frame; the lon/lat→unit convention round-trips through the trusted drill path | Observable (unit) | — | `web/src/camera.test.ts` — `globeCamPose`'s center ray raycast against a real `SphereGeometry`, `hit.uv → uvToWorld`, lands back on the aimed sector center, AND `dot(up,+Y)>0` pins north's direction | flip `lonLatToUnit`'s sign / swap `cross(P,east)`→`cross(east,P)` → the raycast / up-direction assertions trip |
| Drag PANS the sub-point across the surface (grab-the-map sign), scaled by altitude | Observable (unit + e2e) | 4 | `camera.test.ts::panSubPoint` (drag-right → `subLon` decreases; clamps shy of the poles; higher altitude → more travel) + `smoke.spec.ts` flight-pan (`data-sub-lon` moves on a drag) | flip the pan sign → `panSubPoint` sign test trips; freeze the sub-point → e2e poll trips |
| A drilled globe survives an intermediate breadcrumb hop + a regenerate, and returns UPRIGHT to the overview | Observable | 8 / 4 | `smoke.spec.ts::globe drill survives an intermediate crumb hop and a regenerate` (an L1 crumb keeps `#map-content` hidden; Generate clears `data-region`) + the return-upright poll (`data-cam-up-y`≈1 after the Globe crumb) | narrow `navTo`'s guard back to `(nav.level===0\|\|target.level===0)` → intermediate hop leaks the 2D path → trips; drop `exitRegion()` from `enterGlobeView` → stale region survives regenerate → trips; drop `camera.up.set(0,1,0)` in `exitRegion` → `data-cam-up-y`<1 → trips |
| A globe drill lays the refined sector onto a curved high-detail PATCH over the base globe (increment 1b) | Observable + unit; **partly screenshot-only** | 11 (shot) / 4 (e2e) | GEOMETRY (off-GPU): `camera.test.ts::sectorPatchParams` — exact lon/lat ranges, segW/segH aspect tracks phiLength/thetaLength, a raycast centres the patch on the sector (uv≈0.5,0.5) AND off-centre rays pin orientation (north→uv.y>0.5, west→uv.x<0.5). ROUTING (e2e): `smoke.spec.ts` drill asserts `data-patch∈[1-6]` + a DEDICATED `data-patch-textures>0` (showPatch ran, vs the old 2D showSvg); intermediate L2-crumb hop flips `data-patch` 3→2. **CEILING (honest):** `data-patch`/textures prove ROUTING only — texture orientation/mirroring + actual on-screen visibility are pixel-bound and **screenshot-verified ONLY** (a MANDATORY review artifact; green CI ≠ "the patch is visually correct") | remove `scene.add(patch)` → still green (invisible patch — screenshot-only); swap `cross(P,east)` orientation → the off-centre raycast trips; route to `showSvg` instead of `showPatch` → `data-patch` never set → e2e trips |
| A globe drill labels the region with its GROUNDED landmass name (increment 1b-ii billboard) | Observable + unit | 4 (Rust) / 8 (e2e) | NAME (Rust): `continents_spec.rs::continent_at_carries_the_grounded_landmass_name` — every continent hit's `name` is non-empty, ∈ the real `world.continents` names, and ≥2 DISTINCT across the map (data-driven, not constant/empty), PLUS the largest land body resolves to `continents[0].name` (the body→name MAPPING, not just a real name). CONSUMER (e2e): `smoke.spec.ts` — after a drill `.globe-region-label` is visible with non-empty text, and hidden after a regenerate. **CEILING:** the billboard's per-frame projection + horizon-occlusion positioning is screenshot-verified (the name CORRECTNESS is the Rust test's job; the e2e proves the label renders). Render/return-value only — no WorldData/golden change (the name rides `ContinentHit`/`ContinentInfo`, never serialized) | set `name = String::new()` in `continent_at` → Rust non-empty assertion trips; hardcode a constant name → ∈-names / ≥2-distinct trips; nearest→farthest match → the mapping assertion trips (Tkuf vs Dvuv); drop `regionLabel`/`enterRegion` name wiring → e2e label-visible trips |
| Deeper drilling stays in 3D — ONE patch click rebuilds ONE finer patch nested under the parent (increment 1c) | Observable + unit | — / 8 (e2e) | GEOMETRY (off-GPU): `sector.test.ts::patchUvToWorld` (centre→sector centre; u→west/east, (1-v)→north/south; the result through `childSectorAt` is a child INSIDE the parent) + `camera.test.ts` raycast round-trip (a ray at a known patch param point → `hit.uv` → `patchUvToWorld` recovers the world coords — ties the three.js patch uv to the inverse). DRILL (e2e): `smoke.spec.ts::globe drills deeper in 3D` — a patch click takes `data-patch` 3→4 with the sphere shown + `#map-content` hidden + breadcrumb showing `L4` NESTED under the SAME `L3` (the deeper level only the patch-pick path produces, AND the nesting proves it advanced EXACTLY one level — a double-fire would jump to L5 in a different branch). Render-only — no Rust/golden change | flip the `(1-v)` to `v` in `patchUvToWorld` → north/south corner test + raycast pin trip; drop the `onPatchPick`/`patchPickCb` wiring → a patch click is inert → e2e `data-patch` stays 3 → trips; restore the root-only `globeScale && nav.level === 0` guard on the `#map` `drillAt` → the globe-canvas click ALSO bubbles to the `#map` pointerup → DOUBLE drill → `data-patch` 5 (not 4), un-nested → trips |
| Continuous-LOD streaming — after a drill the in-view region fills with high-detail patches, and PANNING keeps the detail (new patches stream in around the sub-point, within caps; out-of-view ones evict) — "scroll around after zooming and keep the map data" (ST-1) | Observable + unit | 8 (e2e) | PURE CORE (off-GPU, off-worker): `lod.test.ts::desiredSectors` (N×N window around the sub-point — window 2 = a 5×5 PREFETCH RING (ST-2), returned NEAREST-FIRST so the load order is breadth-first; NEAREST-point horizon cull — never empties the set at low altitude, the sub-point's own sector always survives; rank keeps the CLOSEST sectors when the cap binds) + `patchcache.test.ts::reconcile` (count cap 32 AND the byte cap BOTH bound `toLoad` — the byte half is independently mutation-pinned; in-view never evicted; out-of-view LRU). CONTRACT (e2e): `smoke.spec.ts::CONTRACT: scrolling after a zoom keeps detail` — after a drill `data-live-patches`≥2 (a SET), then (waiting for the initial batch's keys to STABILISE) a pan makes a genuinely NEW sector key appear in `data-patch-keys` (the PAN's own streaming, not the drill's in-flight tail — the false-green the review caught), still bounded ≤ `MAX_LIVE_PATCHES`, in 3D | drop the `.slice(0, room)` cap in `reconcile` → `toLoad` unbounded → cap test trips; drop `byteRoom` (`room = countRoom`) → byte-firewall test trips; cull on the sector CENTRE not its nearest point → low-altitude set empties → `lod.test.ts` regression test trips; invert the rank sort → cap drops the sub-point's sector → ranking test trips; never call `streamReconcile` on a pan settle → no NEW key appears → CONTRACT e2e trips |
| A lens toggle while DRILLED re-textures the base sphere AND re-streams the in-view patches under the new lens (increment 1e) — closes the stale-`nav.level===0` no-op the review found | Observable | 8 (e2e) | `smoke.spec.ts::globe lens toggle while DRILLED…` — after a drill, toggling Prosperity bumps `data-textures` (base re-textured via `retextureGlobe`) AND `data-refines` (the live patches evicted + re-streamed, each re-rasterized with `withLayerClasses` so the wash group — present in the refined sector's `render_globe_texture` — shows), staying in 3D. **CEILING:** routing only; the wash PIXELS on the patch are screenshot-bounded (same as 1b/1c). Render-only | restore the root-only guard (`globeScale && nav.level === 0`) in `setLayerState` → a drilled lens toggle falls through to the hidden 2D path → `data-textures` never bumps → e2e trips; drop the `refreshGlobePatches()` call → patches keep the old lens → `data-refines` frozen → e2e trips |
| A globe drill lands WHERE YOU CLICKED — within 1°, not a sector-centre away (the precision contract) | Observable (e2e) | 8 @ 2000 | `smoke.spec.ts::a globe drill lands WHERE you clicked` — fresh page, dead-centre click rays to exactly (lon −90°, lat 0°); `data-sub-lon/lat` must land within 1°. The old behaviour centred the camera on the containing SECTOR's geometric centre — measured 25.2° off at L3 (the click's coords were discarded in navTo), marooning the view over the wrong content. Fix: `navTo(target, focus)` threads the click's world point to `enterRegion` (crumb hops still centre on the sector); picks derive uv from the EXACT `hit.point` via `camera.ts::unitToUv` (round-trip pinned), not the raycaster's barycentric `hit.uv` (~4° off mid-triangle on the 64×48 sphere) | `void focus` in navTo's globe branch (snap back to sector centre) → 22.5° → red (mutation-verified) |
| Drilled detail is seamless ACROSS THE ANTIMERIDIAN (the wrap is a seam like any other) | Data | planet 8 @ 2000, L3 (7,4)\|(0,4) | `scale_spec.rs::wrap_adjacent_sectors_agree_at_the_antimeridian` — the wrap-adjacent sector columns agree on the biome along x=0≡W (≥85%, interior-seam parity). Mechanism: `nearest_parent_cells` + `parent_anchor_elevation` + the projections use minimum-image x (`in_rect_p`/`dist2_p` via `plates::wrap_dx`) and an x-unclamped sampling halo when `parent.mesh.periodic` — without it each column saw one-sided context (mutation-measured: **0%** agreement, a total content mismatch at the wrap) | force `period = None` → 0% → red (mutation-verified) |
| Drilling from a scrubbed PAST year is temporally honest — the base SNAPS to the present (tiles always render the present) | Observable | 4 @ 2000 | `smoke.spec.ts::globe time-slider re-textures…` extension: scrub to the past → drill → one more base re-texture fires (the snap); return → the label truthfully reads "present". Without the snap, present-day patches composited over a past base (mixed-era content, empirically confirmed at year 0) and the label lied on return | remove the snap in navTo → the count never bumps → red (mutation-verified) |
| The drill's detail fill shows LIVE progress and a truthful ready state | Observable | 8 @ 2000 | `smoke.spec.ts::drill detail fill shows live progress…` (`data-pending-tiles` appears > 0 then drains to 0; status passes "Loading detail… N tiles" → region prompt) + the mounts test's race-free read: at the moment "Globe ready" shows, `data-textured` is ALREADY "1" (ready is announced only after the texture lands). Lens re-rasters announce "Applying lens…" | remove the updateStreamProgress calls → attr never set → red |
| Polar rows never stream wedge-distorted tiles (|lat| > 75° stays the faded base) | Observable (unit) | — | `lod.test.ts::POLAR CAP` — rows centred beyond ±75° are excluded from the window (equirect tiles there render as converging streaks, screenshot-confirmed; the faded base reads as intended low-fi polar ocean) | drop the cap in desiredSectors → sy=0 re-admitted → red |
| Re-drilling the same point after a return loads the same sector (app state machine round-trip) | Observable | 8 @ 2000 | `smoke.spec.ts::re-drilling the same point…` — drill, drain, return (upright), drill the same screen point: same sub-lon/lat (≤0.02 rad) and identical breadcrumb chain | corrupt nav state on return → crumbs differ → red |
| The perf budget gate RUNS IN CI (ci.yml perf-gate job, PERF_BUDGET_SCALE=2.0) | Determinism (perf) | — | `perf_baseline --check` ran in no CI workflow since Phase 6 — a 50% per-component regression was invisible. Now a CI job; on its FIRST local run it caught the periodic-flip + naming-recal planet-render regression (19→36ms, intrinsic, re-anchored with causes in perf_baseline.rs + docs/perf_baseline.md) | delete the ci.yml job → the gate is gone (review-visible) |
| Visual-regression metrics sit in RECORDED BANDS, not floors | Observable | 42 | `visual_regression.rs` ornate metrics pinned to recorded values ± generous relative bands (water 0.021 ±50%+, ink, warm, variance, mean) — the old floors only caught catastrophe (a 99% ocean loss passed `water > 0.003`) | restore the floor thresholds → a 99% layer loss passes again (review-visible) |
| Drilling shows the SAME place at higher fidelity — the cross-LEVEL contract (zoom refines, never rewrites) | Data | planet 8 @ 2000 L3 (globe path) + 42 continental | `scale_spec.rs::drilling_a_planet_shows_the_same_place_at_higher_fidelity` (land/sea ≥92%, biome ≥75% per sector vs the parent at the same locations) + the TIGHTENED `refined_sector_reproduces_parent_within_tolerance` (MAD<0.05, agree>0.96 — was 0.15/0.90, loose enough to let a tenth of the map flip across a zoom). Mechanism: `scale.rs::parent_anchor_elevation` — the child's elevation ANCHORS to the parent's FINAL eroded field (nearest-parent sample + 2 Jacobi smoothing passes) + zero-mean detail noise; child EROSION REMOVED from the refine path (re-deriving pre-erosion base + own erosion was the rewrite: measured 55–85% land/sea, 34–58% biome agreement on the globe path before). Anchored: 97–100% / 85–90%, MAD ~0.01. Bonus coherence: child valleys now align with the PROJECTED parent rivers; patch edges match the coarse base render. `seed42_sector` golden re-anchored; wasm 5/5 | disable the anchor (return None) → land/sea 55% → red (mutation-verified) |
| Adjacent drilled sectors agree on the BIOME at their seam (no straight-line "nature shifts") | Data | planet 8 @ 2000, L3 (1,4)\|(2,4) | `scale_spec.rs::adjacent_sectors_agree_on_biome_at_their_seam` — the probe-picked worst land seam: ≥90% of 60 edge samples classify identically on both sides. Elevation was already pinned (`pin_edges_to_shared`) but climate was NOT — each sector marches temperature/precipitation independently, and biomes QUANTIZE the disagreement into discrete class swaps along the viewBox line. Fix: `scale.rs::pin_climate_to_parent` blends all 6 climate fields toward the parent's nearest-cell value with the same edge smoothstep, before `biomes::classify` (pure mul/add — refine stays native↔wasm byte-identical; `seed42_sector` golden re-anchored + wasm twin re-verified 5/5). 87% unpinned → 92% pinned (residual = nearest-cell sampling granularity at legitimate transitions) | disable the `pin_climate_to_parent` call → 87% < 90% → red (mutation-verified) |
| The streamed patch set COVERS the visible cap at the drill level (no crisp-vs-blurry straight edge mid-view) | Observable | — | The DOMINANT seam artifact was not data at all: the 5×5 stream window (~112° centred) was smaller than L3's visible cap (~123°), so the patch set's EDGE — crisp detail abutting the blurry coarse base sphere — was permanently inside the view as a sharp straight line. Fix: window 3 (7×7) + caps 52/208MB (`patchcache.ts` math note); screenshot-verified continuous terrain. **CEILING:** coverage is config + screenshot-bound; the e2e bound assertions track `MAX_LIVE_PATCHES` automatically | shrink the window back to 2 → the patch-set edge re-enters the L3 view (screenshot-bound) |
| A globe drill GLIDES into the flight pose (1f entry-ease) — no one-frame snap at fly-to→flight handoff or deeper-drill re-frame | Observable (unit + signal) | — | `camera.test.ts::lerpPose` (endpoints exact, midpoint strictly between, t clamped, up stays unit — a snap-instead-of-glide implementation fails the midpoint test) + `smoke.spec.ts` drill asserts `data-entry-eases > 0` (bumped ONLY when enterRegion starts a glide; mutation-verified: never write the signal → red). **CEILING:** the on-screen smoothness is feel/judgment-bound, like the other globe motion items | drop the `entryEase` capture in `enterRegion` → signal never set → e2e red; break the lerp clamp → unit red |
| The zoom-out overview SIMPLIFIES rivers it can't resolve at scale (cartographic generalisation, ADR 0001 §3) | Observable | 26 | `mapgen-render/src/simplify.rs::tests` (Visvalingam–Whyatt: endpoints kept, collinear/ sub-tolerance vertices dropped, output a subsequence, deterministic) + `generalisation.rs::overview_major_rivers_are_visvalingam_simplified` (the planet render draws FEWER river points than the raw Strahler≥4 cell count — a RELATION, `drawn < raw`, not a magic count; RIVER_SEED moved 11→26 because seed 11's periodic drainage no longer reaches Strahler 4). World-space simplify before projection, so planisphere + globe both benefit; render-only (planet SVG never hashed → no golden move) | remove the `visvalingam` call in `render_major_rivers` → drawn == raw → trips; break endpoint-keeping in the simplifier → unit tests trip |
| The overview COASTLINE is traced into continuous loops + simplified (not per-cell hexagon outlines) | Observable | 26 | `generalisation.rs::overview_coastline_is_traced_and_simplified` — the coast `<g>` is `<polyline>` loops (no `<polygon>`) and its point count is below the land/sea boundary-edge count (`drawn < edges`, relational). Reuses the SHARED `extract_coastline_polylines` (the ornate ripples' tracer, now `pub(crate)`) + `visvalingam`; both planisphere + globe share `render_coast` | restore per-cell `write_polygon_outline` → coast has `<polygon>` → trips; drop the `visvalingam` call → points climb to ~boundary-edge count → trips |
| Globe tiles rasterize OFF the main thread — the worker ships RGBA, the paint thread only blits + builds the patch mesh (the ~300 ms SVG `drawImage` is gone); lenses are honored by usvg, so the worker bakes them | Observable + unit | 42 (native) / 8 (e2e) | `visual_regression.rs::globe_texture_rasterizes_and_its_lens_class_is_honored` — the baseline globe raster vs `with_root_class("on-faith")` differ >1% of bytes, i.e. usvg 0.47 HONORS the `svg.on-faith` selector (the load-bearing fact that lets the worker bake the lens without re-applying it on main) + `mapgen-wasm/tests/render_rgba.rs` (the wasm `renderRgba` returns a sane, FULLY-OPAQUE `w·h·4` buffer; on-faith ≠ baseline) + `smoke.spec.ts::globe scale mounts…` asserts `data-last-blit-ms < 50` (the synchronous paint-thread cost; the GPU `texImage2D` is deferred to the next RAF and not in the span). Cross-platform golden BYTE-IDENTICAL (render_rgba touches no `WorldData`) | no-op `with_root_class` → both lens-differ tests red; revert the worker to ship SVG + main-thread `rasterizeSvg` → `data-last-blit-ms` is null/>50 → e2e red (NOT `data-textures`, which bumps identically on both paths) |
| Detail follows DURING motion (continuous-follow), not only on settle — plus a velocity-predictive ring prefetches one tick ahead, all bounded by the cache firewall and a STREAM_BUDGET-outstanding refill | Observable + unit | 8 (e2e) | `smoke.spec.ts::CONTRACT (Phase B): detail follows DURING a pan` — `data-stream-pumps` climbs BEFORE `mouse.up` (the during-motion pump; un-settled for SETTLE_MS=140 > PUMP_MS=90 ⇒ ≥1 motion-pump per drag, worker-timing-INDEPENDENT) AND a new patch key streams in, live ≤ `MAX_LIVE_PATCHES` + `lod.test.ts::predictedAhead` (stationary ⇒ no ahead sectors; moving ⇒ a leading sector the in-view window omits, ahead-only, EAST overshoot) + `smoke.spec.ts::drill detail fill…` (a STATIC drill fills ≥8 patches — the in-flight budget is REFILLED on each tile completion, not starved at 3). **CEILING (honesty):** discard-stale (drop a tile the camera already PASSED rather than upload-then-LRU-evict it — the wasted-blit stutter Phase B exists to kill) is INSTRUMENTED via `data-tiles-discarded` but is NOT pinned by an assertion: it only fires when a tile lands AFTER its sector has left the desired set, which is worker-timing-dependent and can't be made deterministic the way the pump can (any pointermove holds the camera un-settled past PUMP_MS, guaranteeing a pump; nothing similarly guarantees a stale ARRIVAL). The counter is a live debugging witness, not a gated contract. Likewise the velocity-sampling cadence (prevPosUnit advanced ONLY on the pump, not on the faster tile-completion refills) is asserted only indirectly through `predictedAhead`'s unit test, not by a timing assertion | disable the during-motion pump in globe.ts → `data-stream-pumps` frozen mid-drag → red (mutation-verified); collapse `predicted = pos` → predictedAhead moving-case `[]` → red (mutation-verified); drop the budget refill (the `streamReconcile()` after `showPatch`) → static fill stuck at 3 → red (mutation-verified, measured `data-live-patches`=3) |
| The wasm module stays in a SIZE BAND — the worker-side rasterizer is a bounded +1 MB, not unbounded | Observable | — | `web/scripts/check-bundle.mjs` bands the dist `.wasm` to 1.5–2.8 MB (2.13 MB today: the pipeline + resvg/usvg/tiny-skia, fontless). Mirrors the three-isolation guard: a drifted size fails the build loudly | re-enable usvg `text`/fontdb or add a heavy dep → > 2.8 MB → guard fails; tree-shake the rasterizer away → < 1.5 MB → guard fails |

## Periodic world generation — the seamless globe (Phases 0–6)

The planet/globe is longitude-PERIODIC (a cylinder: wraps in x, clamps at the poles) so the sphere has NO antimeridian seam. Continental worlds (`reference_params`) stay flat. See `docs/design/periodic-world-generation.md`.

| Claim | Layer | Seed | Test | Red-mutation |
|---|---|---|---|---|
| The field is CONTINUOUS across the seam — mesh adjacency wraps, and elevation/precip/flow have no antimeridian band | Data | synthetic + planet | `mesh.rs::{periodic_mesh_wraps_the_seam_and_flat_does_not, neighbor_symmetry_holds_for_flat_and_periodic}` · `plates.rs::{wrap_dx_is_signed_minimum_image, periodic_plate_assignment_wraps_the_seam}` · `noise.rs::periodic_noise_is_continuous_across_the_seam` · `climate.rs::upwind_neighbor_wraps_the_seam` · `hydrology.rs::flow_crosses_the_seam_only_on_a_periodic_mesh` | hardcode `periodic:false` in the ghost build / drop the `wrap_dx` min-image / cos-only noise (mirror planet) / drop the seam-aware `upwind_neighbor` |
| The periodic planet is byte-identical native↔wasm (the seam path is fmath-clean) | Determinism | 9 (planet) | `pipeline_spec.rs::planet_seed9_far_shore_golden_hash` + its twin `cross_platform.rs::planet_seed9_far_shore_golden_matches_native_under_wasm` (re-anchored at the flip; 5/5 wasm pass) | a periodic-path transcendental bypassing `fmath` → wasm hash diverges |
| The CROSSING/SUNDERED/COLONIZE taxonomy was RE-DERIVED for the periodic planet (the carrier claims hold on the new seeds) | Data | 11,19,26,30 / 23,10 / 18,27,32 | the Carrier / Diffusion / Trade rows above, now iterating the re-derived constants (calibrated probe: it reproduced the old flat constants exactly before being trusted on periodic worlds) | (covered by those rows — e.g. `cross_water_conquest_fires_on_every_crossing_seed`) |
| The COLONY far-shore place tag fires on a REAL periodic world | Data | 18,27,32 (fires); 23,10 (absent) | `colony_far_shore_claims.rs::an_overseas_colony_is_tagged_with_the_named_far_shore_it_reached` (mutation-verified) | drop `.far_shore(far_continent)` in `colonization.rs` → no tagged `CityFounded` → red |
| Naming is re-calibrated for the periodic planet: continents down to ~1% of the world are named (not just the dominant few) | Data | 9 (planet) + 42 (continental) | `continents_spec.rs::the_periodic_planet_names_every_continent_down_to_one_percent` (mutation-verified: revert `MIN_CONTINENT_DIVISOR` 100→40 → the named set drops the 1–2.5% band → trips). Pins `world.continents` to EXACTLY the land bodies ≥1% of the world. Single-landmass continental worlds are unaffected (no bodies in the 1–2.5% band → `seed42_*` goldens hold; only `seed9_planet` re-anchored) | revert `MIN_CONTINENT_DIVISOR` to 40 |
| The 3-strand far-shore chronicle (faith+colony+sword on one shore) OCCURS again in a real generated world, after the naming re-calibration | Observable (lore, CLI e2e) | 27 (real) + synthetic (logic) | `mapgen-cli/tests/lore_cli.rs::lore_auto_shore_weaves_the_far_shore_chronicle_through_the_cli` drives the BINARY (`generate --planet --seed 27` → gzip → `lore --event auto-shore`) and asserts all THREE beats name shore "Zuk" — the marquee chronicle, proven to occur end-to-end. The weave LOGIC is separately pinned DRIFT-IMMUNELY by the synthetic `shore.rs::{select_shore_picks_a_three_strand_far_shore…, the_far_shore_chronicle_weaves_all_three_strands…}` (`three_strand_world()`). NB: the 3-strand is RARE under periodicity (colony settles unclaimed fringes, conquest seizes owned anchors — they seldom share a continent; ~6 seeds in 0..200), roughly as rare as on the flat world — so seed 27 is a behavioral fixture (re-derive if a sim change shifts it; the synthetic guard holds regardless) | drop `.far_shore` in colonization.rs/mearsheimer.rs → seed 27 loses the colony/sword beat → lore_cli trips; drop a strand event from `three_strand_world()` → `distinct_strands` 2 → shore.rs trips |
| The GLOBE is seamless — the antimeridian fade band is gone; the LOD longitude window wraps (latitude still clamps) | Observable | — (unit + screenshot) | `lod.test.ts::{WRAPS longitude across the antimeridian, CLAMPS latitude at the poles}` (the window stitches sx=0↔span-1; latitude never wraps top↔bottom). Base-globe seam removal = `globe.ts fadePoleCaps` (seam bands dropped, `RepeatWrapping` joins the continuous edges) — screenshot-confirmed at the former antimeridian | revert `lod.ts` `sx` to clamp → no `sx≥span-2` near the seam → the WRAPS test trips |
| Drilled streaming STITCHES across the seam — detail patches on both sides of lon ±180 stream together (no gap when you zoom in at the antimeridian) | Observable | 8 (spot-checked) | the standing guard is the `lod.test.ts` wrap above; the integrated path was confirmed by a throwaway e2e (live `data-patch-keys` columns straddle sx=0 AND sx=span-1 with the drilled sub-point at the seam) + a screenshot. **CEILING:** screenshot/throwaway-verified, not a standing e2e | n/a — the standing unit wrap is the regression guard |

## Render invariants

| Claim | Layer | Seed | Test | Red-mutation |
|---|---|---|---|---|
| Biomes SVG well-formed; polygon count = cell count; no NaN coords | Observable | 42 | `mapgen-render/tests/svg_invariants.rs::{svg_envelope_is_well_formed, polygon_count_matches_cell_count, no_nan_coordinates_in_output}` | emit one stray `<rect>` / a NaN coord |
| Ornate / planet / every preset rasterize to a sane image | Observable | 42 | `mapgen-cli/tests/visual_regression.rs::{ornate_render…, planet_render…, every_preset…}` | render a blank/uniform canvas |
| Ornate draws borders between adjacent polities | Observable | 42 | `svg_invariants.rs::ornate_antique_draws_borders_between_adjacent_polities` | skip the border layer |

---

## Open gaps (the work this registry exposes)

1. ~~**Observable — exclave distinctness**~~ *(CLOSED, substage 4)*: `recolor_political`
   greedy-graph-colors the polity adjacency post-history; `political_legibility.rs`
   pins it (mutation-verified). Goldens re-anchored (delta proven to be `nation.color`
   only). ~~**Residual:** an automated e2e that points at *the* exclave region~~ — now
   CLOSED: the wash exposes `data-polity` realm groups + `.exclave` tags, and
   `smoke.spec.ts::scrubbing the slider reveals an overseas exclave region` asserts it
   (seed 26 @ 2000: exclave present at the present, absent at the founding min).
2. ~~**Replay — earned crossing → year-specific flip**~~ *(CLOSED, substages 3 + DOM-surfacing)*:
   the Rust side via `the_earned_crossing_replays_faithfully_in_the_time_slider`, and the
   **web/DOM** half via the exclave-reveal e2e above (no longer blocked — legibility +
   `data-polity` realm groups landed).
3. ~~**Data — sundered seeds grow no crossable lane**~~ *(CLOSED, substage 2)*: the
   *absent* half is now pinned in both directions by `sundered_lanes_claims.rs`,
   mutation-verified.
4. ~~**Data — both-tier substrate across all crossing seeds**~~ *(CLOSED)*: all four
   crossing seeds now assert a crossable strait + an open-ocean wall. (Exact
   per-seed crossing *counts* are intentionally never pinned — brittle.)

## How to extend this file

When you add a feature, add its rows here *in the same change* as its tests:
name the layer, the canonical seed, the test `path::fn`, and the red-mutation you
verified flips it red. When you claim something is done, grep this file first —
if the row says GAP at that layer, the claim is wrong.
